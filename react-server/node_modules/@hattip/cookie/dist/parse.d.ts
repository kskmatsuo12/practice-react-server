import { CookieParseOptions } from 'cookie';
export { CookieParseOptions } from 'cookie';

declare module "@hattip/compose" {
    interface RequestContextExtensions {
        /** Incoming cookies parsed into an object */
        readonly cookie: Record<string, string | undefined>;
    }
}
type CookieParserContext = {
    request: Pick<Request, "headers">;
};
/**
 * Create a cookie parser middleware.
 *
 * @param options Cookie parser options
 *
 * @returns A middleware that will parse cookies
 */
declare function cookieParser(options?: CookieParseOptions): (ctx: CookieParserContext) => void;

export { CookieParserContext, cookieParser };
